# Porta HTTP do microserviço
PORT=3000

# Segredo para assinar os JWT (HS256). TROQUE por um valor forte e único.
JWT_SECRET=troque-por-um-segredo-forte

# (Opcional) Senha do certificado mTLS, se o .pfx/.p12/.pem for protegido.
# CERT_PASSPHRASE=
